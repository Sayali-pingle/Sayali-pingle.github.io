<div class="company-logos-container">
  <div class="company-logos">
    {% for company in site.data.companies %}
      <div class="logo-item">
        <img src="{{ company.logo | relative_url }}" alt="{{ company.name }}" title="{{ company.name }}">
      </div>
    {% endfor %}
    <!-- Duplicate logos for seamless scrolling -->
    {% for company in site.data.companies %}
      <div class="logo-item">
        <img src="{{ company.logo | relative_url }}" alt="{{ company.name }}" title="{{ company.name }}">
      </div>
    {% endfor %}
  </div>
</div>